namespace Cars.Core.Domain.Enums
{
    public enum Category : byte
    {
        Sedan = 1,
        Hetchback,
        Sport,
        MiniVan,
        PickUp,
        SuperCar

    }

    public enum Type : byte
    {
        Benzin = 10,
        Dizel = 20,
        Hybride = 30,
        Gas = 40
    }
}
